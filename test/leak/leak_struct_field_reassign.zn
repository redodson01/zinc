struct Container {
    var name: String
    var items: int[]
}

func main() {
    var c = Container(name: "start" + "", items: [1, 2])
    var i = 0
    while i < 100000 {
        c.name = "iter" + " " + "val"
        c.items = [i, i + 1]
        i = i + 1
    }
    0
}
