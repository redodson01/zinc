class Inner {
    var value: int
}

class Outer {
    var child: Inner
}

func main() {
    var o = Outer(child: Inner(value: 0))
    var i = 0
    while i < 100000 {
        o.child = Inner(value: i)
        i = i + 1
    }
    0
}
