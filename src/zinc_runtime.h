/*
 * Zinc runtime library — embedded into generated C files at build time.
 * This file is converted to a C string by the Makefile and #included by codegen.c.
 *
 * Populated as language features are added.
 */
